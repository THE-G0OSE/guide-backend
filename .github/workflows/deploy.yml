name: deploy via ssh

on:
    push:
        branches: 
            [main]

jobs:
    build-and-deploy:
        runs-on: ubuntu-latest

        steps: 
        - uses: actions/checkout@v4

        - name: set up go
          uses: actions/setup-go@v5
          with: 
            go-version: 1.24.2
        
        - name: build binary
          run: |
            CGO_ENABLED=0 GOOS=linux GOARCH=amd64 \
            go build -ldflags="-s -w" -o myapp

        - name: Deploy via SSH
          uses: appleboy/ssh-action@v1.0.0
          env: 
            HOST_KEY_CHECKING: false
          with: 
            host: ${{ secrets.VPS_HOST }}
            port: ${{ secrets.VPS_PORT }}
            username: ${{ secrets.VPS_USER }}
            key: ${{ secrets.SSH_KEY }}
            script: |
              set -e
              sudo systemctl stop guide-backend || true

              rsync -avz --progress "$GIHUB_WORKSPACE/myapp" ${{ secrets.VPS_USER}}@${{ secrets.VPS_HOST }}:${{ secrets.APP_DIR }}/myapp.new
              
              ssh -o StrictHostKeyChecking=no ${{ secrets.VPS_USER }}:${{ secrets.VPS_HOST }} "mv ${{secrets.APP_DIR }}/myapp.new ${{ secrets.APP_DIR }}/guide-backend && chmod +x ${{ secrets.APP_DIR }}/guide-backend"
              
              sudo systemctl daemon-reload
              sudo systemctl start guide-backend

            