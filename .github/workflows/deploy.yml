name: deploy via ssh

on:
    push:
        branches: 
            [main]

jobs:
    build-and-deploy:
        runs-on: ubuntu-latest

        steps: 
        - uses: actions/checkout@v4

        - name: set up go
          uses: actions/setup-go@v5
          with: 
            go-version: 1.24.2
        
        - name: build binary
          run: |
            CGO_ENABLED=0 GOOS=linux GOARCH=amd64 \
            go build -ldflags="-s -w" -o myapp

        - name: Deploy via SSH
          uses: appleboy/ssh-action@v1.0.0
          env: 
            HOST_KEY_CHECKING: false
          with: 
            host: ${{ secrets.VPS_HOST }}
            port: ${{ secrets.VPS_PORT }}
            username: ${{ secrets.VPS_USER }}
            key: ${{ secrets.SSH_KEY }}
            script: |
              set -e
              sudo systemctl stop guide-backend || true

              scp -o StrictHostKeyChecking=no -P ${{ secrets.VPS_PORT }} \
              "$GITHUB_WORKSPACE/myapp" ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}:/opt/guide-backend/guide-backend.new

              ssh -o StrictHostKeyChecking=no -p ${{ secrets.VPS_PORT }} \
              ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }} \
              "mv /opt/guide-backend/guide-backend.new /opt/guide-backend/guide-backend && chmod +x /opt/guide-backend/guide-backend"

              sudo systemctl daemon-reload
              sudo systemctl start guide-backend

            